export const contextReducer = (state, action) => {
    switch (action.type) {
        case "ADD_TO_NOTES":
            
            return;
        case "DELETE_FROM_NOTES":
            
            return;
    
        default:
            return;
    }
}